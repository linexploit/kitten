<h1>Commande #<%= @order.id %></h1>

<ul>
  <% @order.items.each do |item| %>
    <li><%= item.title %> - <%= number_to_currency(item.price) %></li>
  <% end %>
</ul>

<p>Total: <%= number_to_currency(@order.items.sum(:price)) %></p>
